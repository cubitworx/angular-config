{
  "name": "angular-config",
  "version": "0.0.1",
  "license": "MIT",
  "description": "Angular config service",
  "scripts": {
    "build": "tsc",
    "lint": "tslint ./src/**/*.ts -t verbose",
    "precommit": "npm run build && npm run lint && npm run test:once",
    "test": "npm run build && jasmine",
    "test:once": "jasmine"
  },
  "main": "./dist",
  "types": "./src",
  "author": "Charles Louw <charles.louw@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/cubitworx/angular-config.git"
  },
  "dependencies": {
    "@angular/core": "^4.0.0",
    "reflect-metadata": "0.1.9",
    "rxjs": "^5.2.0",
    "zone.js": "^0.8.5"
  },
  "devDependencies": {
    "@types/jasmine": "2.5.36",
    "jasmine": "^2.5.3",
    "jasmine-core": "^2.5.2",
    "tslint": "^4.5.1",
    "typescript": "~2.2.1"
  }
}
